# CLAUDE.md

本檔案是給 Claude Code 讀取的專案規則與架構說明。**開始任何工作前,先讀
`PROGRESS.md` 了解目前實作進度**,再回來看這份文件確認規則。

## 專案概觀

摩斯外賣銷售記錄 App —— 個人側專案,用於記錄早班外賣工作(捷運站小推車
販售預先做好的三明治、漢堡、飲料)的每日銷售狀況。單人使用,無需登入、
無需雲端同步,目標是隔天就能實際上工使用。

完整功能規格與資料模型見 `docs/spec.md`,此處只列出實作時必須遵守的
架構規則與限制。

## 技術棧

- 前端:React(建議用 Vite 建置,開發體驗快、設定簡單)
- 打包:Capacitor,產出可安裝的 Android APK
- 儲存:裝置本機儲存(localStorage 或 Capacitor Preferences,擇一)
- UI 語言:繁體中文
- 程式碼與註解:英文

## 建議架構(非強制規則,方便維護用)

單人、單裝置、純本機儲存,不需要 3-tier 架構或多個獨立 service。
沒有硬性禁止規則,但建議依關注點分離,結構清楚、好維護:

- `components/` — 畫面元件(今天頁籤、昨天頁籤、新增/編輯表單、
  管理常用品項等),負責渲染與事件綁定
- 業務邏輯(新增/編輯/刪除品項、+1/-1 計算、換日判斷、套用常用品項
  範本)建議抽成純函式或 custom hook,和畫面元件分開,方便測試與複用
- `services/storage.js` — 讀寫本機儲存的建議統一入口

這只是建議的組織方式,不是必須遵守的限制,依實際開發彈性調整即可。

## 資料模型摘要

- 一般品項:`stock` / `price` / `sold`,剩餘 = stock - sold
- 飲料品項:共用 `stock`,`comboPrice` / `aLaCartePrice`,
  `comboSold` / `aLaCarteSold`,剩餘 = stock - (comboSold + aLaCarteSold)
- 常用品項範本:獨立儲存,不受換日輪替影響
- 換日邏輯:開啟 App 時檢查裝置日期,今天→昨天→前天(前天逾期捨棄),
  未換日則維持現狀

完整欄位定義與 JSON 範例見 `docs/spec.md`。

## 開發優先順序

若時間不足,優先順序:
1. 「今天」頁籤功能完整(新增/編輯/刪除/計數/即時存檔)
2. `storage.js` 換日輪替邏輯
3. 「昨天」頁籤(唯讀顯示)
4. 「管理常用品項」範本功能
5. Capacitor 打包與 APK 產出

## 交付項目

1. 可運作的網頁版本(先在瀏覽器測試)
2. Capacitor 打包後可安裝到 Samsung Android 手機的 .apk
3. 簡單的建置/安裝步驟說明(給非 Android 開發背景的使用者看)

## Tracker 使用規則

每完成一個「可獨立驗證的小任務」(例如某檔案的某功能可運作、某頁籤
UI 完成、打包成功),更新 `PROGRESS.md` 的對應項目,不需要每改一行
程式碼就更新。
